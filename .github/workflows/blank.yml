name: blank
on:
  push:
  workflow_dispatch:

jobs:
  shell:
    runs-on: ubuntu-latest
    if: |
      'a' || 'b' || 'c' == 'c' && 'd' == 'd'
    steps:
      - name: test
        run: |
          uname -a
