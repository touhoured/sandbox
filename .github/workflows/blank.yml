name: CI

on:
  push:

jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: ajilraju/actions-date@v0.1
          with:
            args: date +%F
