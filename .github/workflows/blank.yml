name: blank
on:
  push:
  workflow_dispatch:

env:
  a: a
  b: b
  c: c
  d: d

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - name: test
        if: |
          (env.a == env.b == env.c == 'c') &&
          env.d == 'd'
        run: |
          uname -a
