name: blank
on:
  push:
  workflow_dispatch:

jobs:
  shell:
    runs-on: ubuntu-latest
    if: |
      'a' || 'b' || 'c' == 'a' && 'd' == 'd'
    steps:
      - name: test
        run: |
          uname -a
